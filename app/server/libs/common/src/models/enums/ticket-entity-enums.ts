export enum TicketCategoryEnum {
  SUPER_EARLY_BIRD = 'Super Early Bird',
  EARLY_BIRD = 'Early Bird',
  STANDARD = 'Standard',
  LAST_MINUTE = 'Last Minute',
  VIP = 'VIP',
  CUSTOM = 'Custom',
}

export enum SalesStrategyEnum {
  FCFS = 'First Come First Served',
  TIMED_RELEASE = 'Timed Release',
}

export enum WaitListStatusEnum {
  WAITING = 'WAITING',
  NOTIFIED = 'NOTIFIED',
  REMOVED = 'REMOVED',
}
